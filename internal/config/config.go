// Package config will load and validate Ganimedes' configuration: which MCP
// server to wrap, which tools are denied, and which require human approval.
//
// No logic yet. Implementation lands alongside build-order step 1.
package config
